let User;_402‍.x([["findAll",()=>findAll],["findOne",()=>findOne],["update",()=>update]]);_402‍.w("../models/user-model",[["default",["User"],function(v){User=v}]]);

       const findAll = async (_req, res) => {
    try {
        const response = await User.find().sort({ createdAt: "desc" });
        return res.status(200).json(response);
    } catch (err) {
        return res.status(400).json({
            status: 400,
            message: err.message,
        });
    }
}

       const findOne = async (req, res) => {
    try {
        const { user } = req.user;
        return res.status(200).json({
            status: true,
            result: user
        });
    } catch (err) {
        return res.status(400).json({
            status: 400,
            message: err.message,
        });
    }
}

       const update = async (req, res) => {

    const id = req.params.id;
    _402‍.g.console.lob("id", req.params.id);
    try {
        await User.findByIdAndUpdate(id, req.body, {
            useFindAndModify: false,
        });
        return res.status(200).json({
            status: 200,
            message: "User updated",
        });
    } catch (err) {
        return res.status(500).json({
            message: err.message
        });
    }
}