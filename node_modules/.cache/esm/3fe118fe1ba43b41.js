let mongoose;_1ba‍.x([["default",()=>_1ba‍.o]]);_1ba‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);
const { Schema } = mongoose
const { ObjectId } = mongoose.Schema

const catalogSchema = new Schema({
    service: {
        type: mongoose.Schema.Types.ObjectId,
        ref: 'Service'
    },
    user: {
        type: mongoose.Schema.Types.ObjectId,
        ref: "Users"
    },
    image: {
        type: String,
    },
    status: {
        type: Boolean
    }
},
    { timestamps: true }
);

_1ba‍.d(mongoose.model('Catalog', catalogSchema));