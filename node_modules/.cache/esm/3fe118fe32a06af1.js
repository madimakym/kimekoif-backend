let mongoose;_32a‍.x([["default",()=>_32a‍.o]]);_32a‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose
const { ObjectId } = mongoose.Schema

const catalogSchema = new Schema({
    libelle: {
        type: String,
        required: "Libelle is required"
    },
    content: {
        type: String,
        required: "Content is required",
        maxlength: 10000,
    },
    postedBy: {
        type: ObjectId,
        ref: "Users"
    },
    image: {
        data: Buffer,
        contentType: String
    }
},
    { timestamps: true }
);

_32a‍.d(mongoose.model('Catalog', catalogSchema));