let Catalog,fs;_597‍.x([["create",()=>create],["findAll",()=>findAll]]);_597‍.w("../models/catalog-model",[["default",["Catalog"],function(v){Catalog=v}]]);_597‍.w("fs",[["default",["fs"],function(v){fs=v}]]);


       const create = async (req, res) => {
    try {
        let fields = req.fields;
        let files = req.files;
        let catalog = new Catalog(fields);
        if (files.image) {
            catalog.image.data = fs.readFileSync(files.image.path);
            catalog.image.contentType = files.image.type;
        }

        catalog.save((err, result) => {
            if (err) {
                _597‍.g.console.log("saving Catalog ==>", err);
                res.status(400).send("Error Saving");
            }
            res.json(result);
        })

    } catch (err) {
        _597‍.g.console.log("error ==>", err);
        res.status(400).json({
            err: err.message
        });
    }
};

       const findAll = async (req, res) => {
    try {
        const response = await Catalog.find();
        return res.status(200).json(response);
    } catch (err) {
        return res.status(400).json({
            status: 400,
            message: err.message,
        });
    }
    // console.log("user ===> ", req.user)

}