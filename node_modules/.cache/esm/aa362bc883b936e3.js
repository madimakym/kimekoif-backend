let User,jwt;_83b‍.x([["findAll",()=>findAll],["findOne",()=>findOne],["update",()=>update]]);_83b‍.w("../models/user-model",[["default",["User"],function(v){User=v}]]);_83b‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);


       const findAll = async (_req, res) => {
    try {
        const response = await User.find().sort({ createdAt: "desc" });
        return res.status(200).json(response);
    } catch (err) {
        return res.status(400).json({
            status: 400,
            message: err.message,
        });
    }
}

       const findOne = async (req, res) => {
    const id = req.params.id;
    try {
        const user = await User.findById(id);
        return res.status(200).json({
            status: true,
            result: user
        });
    } catch (err) {
        return res.status(400).json({
            status: 400,
            message: err.message,
        });
    }
}

       const update = async (req, res) => {
    const id = req.params.id;
    try {
        let user = await User.findByIdAndUpdate(id, req.body, {
            useFindAndModify: false,
        });
        return res.status(200).json({
            success: true,
            result: user,
        });
    } catch (err) {
        return res.status(500).json({
            message: err.message
        });
    }
}